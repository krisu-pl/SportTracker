<div class="padding-20">
    <b>Choose event:</b>
    <br><br>

    <select id="simulate-choose_event">
        <option selected disabled>Choose event</option>
        {{#each events}}
            <option value="{{id}}">
                {{name}}
            </option>
        {{/each}}
    </select>
</div>